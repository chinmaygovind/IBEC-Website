services:
  - type: web
    name: ibec-website
    runtime: python
    rootDir: build
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn server:app
    envVars:
      - key: PYTHON_VERSION
        value: "3.11.6"
